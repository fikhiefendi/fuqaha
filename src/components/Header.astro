---
import { SITE } from '../site.config';
import { LANG_META, LOCALES, NAV, useT, type Lang } from '../i18n/ui';
import { href, switchLang } from '../lib/url';
import Mark from './Mark.astro';

interface Props {
  lang: Lang;
  /** On the home page the header sits transparently over the hero. */
  home: boolean;
}
const { lang, home } = Astro.props;
const t = useT(lang);
const path = Astro.url.pathname;
---

<header class="site-header" class:list={{ overlay: home }} data-header>
  <div class="wrap bar">
    <a href={href(lang)} class="logo" aria-label={SITE.name}>
      <span class="logo-mark"><Mark size={34} /></span>
      <span class="logo-name">{SITE.name}</span>
      <span class="logo-sub" lang="ar">{SITE.nameAr}</span>
    </a>

    <nav class="nav" aria-label={t('menu')} id="site-nav">
      {
        NAV.map((item) => {
          const target = href(lang, item.path);
          return (
            <a href={target} aria-current={path.startsWith(target) ? 'page' : undefined}>
              {t(item.key)}
            </a>
          );
        })
      }
    </nav>

    <div class="tools">
      <a href={href(lang, 'search/')} class="icon" aria-label={t('nav.search')}>
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"
          ><circle cx="11" cy="11" r="7"></circle><path d="m20 20-3.5-3.5"></path></svg
        >
      </a>
      <button type="button" class="icon theme" data-theme-toggle aria-label={t('theme.toggle')} title={t('theme.toggle')}>
        <svg class="moon" width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M20 14.5A8 8 0 0 1 9.5 4a8 8 0 1 0 10.5 10.5z"></path></svg>
        <svg class="sun" width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><circle cx="12" cy="12" r="4"></circle><path d="M12 2v2m0 16v2M4.9 4.9l1.4 1.4m11.4 11.4 1.4 1.4M2 12h2m16 0h2M4.9 19.1l1.4-1.4m11.4-11.4 1.4-1.4"></path></svg>
      </button>
      <span class="slash" aria-hidden="true">/</span>
      <details class="lang">
        <summary aria-label="Language">
          {LANG_META[lang].short}
          <svg width="10" height="10" viewBox="0 0 10 10" aria-hidden="true"><path d="m2 3.5 3 3 3-3" fill="none" stroke="currentColor" stroke-width="1.5"></path></svg>
        </summary>
        <div class="lang-menu">
          {
            LOCALES.map((l) => (
              <a href={switchLang(path, l)} hreflang={l} lang={l} aria-current={l === lang ? 'true' : undefined}>
                {LANG_META[l].name}
              </a>
            ))
          }
        </div>
      </details>
      <button class="burger" type="button" aria-controls="site-nav" aria-expanded="false" aria-label={t('menu')}>
        <span></span><span></span><span></span>
      </button>
    </div>
  </div>
</header>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 1000;
    background: var(--bg);
    box-shadow: 0 1px 0 var(--rule);
    transition:
      background 0.3s,
      box-shadow 0.3s;
  }
  /* Transparent over the home hero until the page scrolls */
  .site-header.overlay {
    position: fixed;
    inset-inline: 0;
    background: transparent;
    box-shadow: none;
    color: #fff;
  }
  .site-header.overlay.scrolled {
    background: var(--bg);
    color: var(--ink);
    box-shadow: 0 1px 0 var(--rule);
  }

  .bar {
    display: flex;
    align-items: center;
    gap: 24px;
    height: var(--header-h);
  }

  .logo {
    display: flex;
    align-items: center;
    gap: 10px;
    color: var(--ink);
  }
  .overlay:not(.scrolled) .logo {
    color: #fff;
  }
  .logo-mark {
    display: grid;
    place-items: center;
    color: var(--rubric);
  }
  .overlay:not(.scrolled) .logo-mark {
    color: inherit;
  }
  .logo-name {
    font: 600 1.55rem/1 var(--serif-latin);
    letter-spacing: 0.01em;
  }
  .logo-sub {
    padding-inline-start: 12px;
    border-inline-start: 1px solid currentColor;
    font: 700 1.35rem/1.2 var(--arabic);
    opacity: 0.85;
  }

  .nav {
    display: flex;
    align-items: center;
    gap: 6px;
    margin-inline-start: auto;
  }
  .nav a {
    position: relative;
    padding: 10px 12px;
    font: 500 0.95rem/1 var(--sans);
    color: inherit;
  }
  .nav a::after {
    content: '';
    position: absolute;
    inset-inline: 12px;
    bottom: 2px;
    height: 2px;
    background: var(--rubric);
    transform: scaleX(0);
    transition: transform 0.25s;
  }
  .nav a:hover::after,
  .nav a[aria-current='page']::after {
    transform: scaleX(1);
  }
  .overlay:not(.scrolled) .nav a::after {
    background: #fff;
  }
  :root[lang='ar'] .nav a {
    font-size: 1.02rem;
  }

  .tools {
    display: flex;
    align-items: center;
    gap: 8px;
    font: 700 0.8rem var(--sans);
  }
  .icon {
    display: grid;
    place-items: center;
    width: 32px;
    height: 32px;
  }
  .slash {
    opacity: 0.6;
  }
  .theme {
    padding: 0;
    border: 0;
    background: none;
    color: inherit;
    cursor: pointer;
  }
  .theme .sun,
  :global(:root[data-theme='dark']) .theme .moon {
    display: none;
  }
  :global(:root[data-theme='dark']) .theme .sun {
    display: block;
  }
  .lang {
    position: relative;
  }
  .lang summary {
    display: flex;
    align-items: center;
    gap: 4px;
    cursor: pointer;
    list-style: none;
    padding: 6px 2px;
  }
  .lang summary::-webkit-details-marker {
    display: none;
  }
  .lang-menu {
    position: absolute;
    inset-inline-end: 0;
    top: calc(100% + 8px);
    min-width: 140px;
    padding: 6px 0;
    background: var(--surface);
    box-shadow: 0 10px 30px rgb(0 0 0 / 0.15);
    color: var(--ink);
  }
  .lang-menu a {
    display: block;
    padding: 8px 16px;
    font: 500 0.88rem var(--sans);
  }
  .lang-menu a[lang='ar'] {
    font-family: var(--arabic-ui);
  }
  .lang-menu a:hover,
  .lang-menu a[aria-current] {
    background: var(--soft);
    color: var(--accent);
  }

  .burger {
    display: none;
    flex-direction: column;
    justify-content: center;
    gap: 5px;
    width: 38px;
    height: 38px;
    padding: 0 8px;
    background: transparent;
    border: 0;
    color: inherit;
    cursor: pointer;
  }
  .burger span {
    height: 2px;
    background: currentColor;
  }

  @media (max-width: 1020px) {
    .burger {
      display: flex;
    }
    .tools {
      margin-inline-start: auto;
    }
    .nav {
      position: absolute;
      inset-inline: 0;
      top: var(--header-h);
      flex-direction: column;
      align-items: stretch;
      gap: 0;
      padding: 8px 16px 16px;
      background: var(--surface);
      color: var(--ink);
      box-shadow: 0 12px 24px rgb(0 0 0 / 0.1);
      display: none;
    }
    .nav.open {
      display: flex;
    }
    .nav a {
      padding: 14px 4px;
      border-block-end: 1px solid var(--rule);
      font-size: 0.85rem;
    }
    .logo-mark {
      width: 40px;
      height: 40px;
    }
    .logo-name {
      font-size: 1.15rem;
    }
  }
</style>

<script>
  const header = document.querySelector<HTMLElement>('[data-header]');
  if (header?.classList.contains('overlay')) {
    const onScroll = () => header.classList.toggle('scrolled', window.scrollY > 40);
    onScroll();
    window.addEventListener('scroll', onScroll, { passive: true });
  }

  const burger = document.querySelector<HTMLButtonElement>('.burger');
  const nav = document.getElementById('site-nav');
  burger?.addEventListener('click', () => {
    const open = nav?.classList.toggle('open') ?? false;
    burger.setAttribute('aria-expanded', String(open));
    header?.classList.toggle('scrolled', open || window.scrollY > 40);
  });

  document.querySelector('[data-theme-toggle]')?.addEventListener('click', () => {
    const root = document.documentElement;
    const next = root.dataset.theme === 'dark' ? 'light' : 'dark';
    root.dataset.theme = next;
    try {
      localStorage.setItem('theme', next);
    } catch {
      /* private mode: the choice lasts for this page only */
    }
  });

  // Close the language menu when clicking elsewhere.
  document.addEventListener('click', (e) => {
    const lang = document.querySelector<HTMLDetailsElement>('.lang');
    if (lang?.open && !lang.contains(e.target as Node)) lang.open = false;
  });
</script>
